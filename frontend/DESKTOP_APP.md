# TelecomApp - Десктопное приложение

Это десктопное приложение на базе Tauri для отслеживания работ телекоммуникационной компании.

## 📋 Содержание

- [Сборка](#сборка)
- [Установка](#установка)
- [Обновление](#обновление)
- [Настройка автообновления](#настройка-автообновления)

---

## 🔨 Сборка

### Локальная сборка (требуется Windows для Windows-версии)

```bash
# Установить зависимости
cd frontend
npm install

# Сборка для текущей платформы
npm run tauri:build
```

### Автоматическая сборка через GitHub Actions

1. Загрузите код на GitHub
2. GitHub Actions автоматически соберёт приложение для Windows и Linux
3. Готовые файлы будут в разделе Releases

---

## 📥 Установка

### Windows

1. Скачайте `TelecomApp_X.X.X_x64-setup.exe` из Releases
2. Запустите установщик
3. Следуйте инструкциям мастера установки
4. Приложение установится в `C:\Program Files\TelecomApp`

**Альтернатива (portable):**
- Скачайте `TelecomApp_X.X.X_x64_portable.exe`
- Запустите без установки

### Linux

**DEB (Ubuntu/Debian):**
```bash
sudo dpkg -i telecomapp_X.X.X_amd64.deb
sudo apt-get install -f  # установить зависимости
```

**AppImage (portable):**
```bash
chmod +x telecomapp_X.X.X_amd64.AppImage
./telecomapp_X.X.X_amd64.AppImage
```

---

## 🔄 Обновление

### Автоматическое обновление

Приложение автоматически проверяет обновления при запуске.

Если доступно обновление:
1. Появится диалоговое окно
2. Нажмите "Обновить"
3. Приложение скачает и установит обновление
4. Перезапустите приложение

### Ручное обновление

1. Скачайте новую версию из Releases
2. Установите поверх старой (Windows) или замените файл (AppImage)

---

## ⚙️ Настройка автообновления

### Шаг 1: Генерация ключей

```bash
cd frontend
chmod +x scripts/generate-keys.sh
./scripts/generate-keys.sh
```

### Шаг 2: Добавление ключей в GitHub

1. Откройте репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Добавьте:
   - `TAURI_PRIVATE_KEY` - содержимое `.tauri/keys/private.key`
   - `TAURI_KEY_PASSWORD` - пароль, введённый при генерации

### Шаг 3: Обновление конфигурации

Добавьте публичный ключ в `src-tauri/tauri.conf.json`:

```json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://your-server.com/updates/{{target}}/{{arch}}/{{current_version}}"
      ],
      "pubkey": "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ"
    }
  }
}
```

### Шаг 4: Размещение обновлений на сервере

Создайте JSON-файл на вашем сервере:

```json
{
  "version": "0.2.0",
  "notes": "Исправлены ошибки, добавлены новые функции",
  "pub_date": "2026-02-20T12:00:00Z",
  "platforms": {
    "windows-x86_64": {
      "signature": "ПОДПИСЬ_ФАЙЛА",
      "url": "https://your-server.com/releases/TelecomApp_0.2.0_x64-setup.exe"
    },
    "linux-x86_64": {
      "signature": "ПОДПИСЬ_ФАЙЛА",
      "url": "https://your-server.com/releases/telecomapp_0.2.0_amd64.deb"
    }
  }
}
```

---

## 🏗️ Структура проекта

```
frontend/
├── src/                    # React приложение
├── src-tauri/              # Tauri (Rust)
│   ├── src/
│   │   ├── main.rs         # Точка входа
│   │   └── lib.rs          # Плагины и логика
│   ├── Cargo.toml          # Зависимости Rust
│   └── tauri.conf.json     # Конфигурация Tauri
├── .github/
│   └── workflows/
│       └── build.yml       # CI/CD для сборки
└── scripts/
    └── generate-keys.sh    # Генерация ключей
```

---

## 🔧 Команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск веб-версии |
| `npm run tauri:dev` | Запуск десктопной версии (dev) |
| `npm run tauri:build` | Сборка десктопного приложения |
| `npm run build` | Сборка веб-версии |

---

## ❓ Частые вопросы

**Q: Приложение не запускается на Windows**
A: Убедитесь, что установлен Microsoft Edge WebView2 Runtime (обычно уже есть в Windows 10/11)

**Q: Как проверить обновления вручную?**
A: В приложении откройте меню "Справка" → "Проверить обновления"

**Q: Где хранятся данные приложения?**
A: 
- Windows: `%APPDATA%\com.telecom.tracking`
- Linux: `~/.config/com.telecom.tracking`